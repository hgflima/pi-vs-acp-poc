---
description: "Generate or update a professional GitHub README.md file with badges, architecture diagrams, screenshots, and best practices."
---

Use the Skill tool to invoke the `github-readme-writer` skill, passing any user-provided arguments (e.g., project URL, template preference) as the `args` parameter.

```
Tool: Skill
Args: { "skill": "github-readme-writer", "args": "$ARGUMENTS" }
```
