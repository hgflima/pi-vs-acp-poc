---
phase: 8
slug: oauth-connection-ui
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-06
revised: 2026-04-06
revision: 2
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for the OAuth Connection UI. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova (neutral base) |
| Component library | base-ui (from components.json) |
| Icon library | lucide-react |
| Font | Geist Variable (sans-serif) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gap inside badges, inline icon padding |
| sm | 8px | Gap between badge and status text, inner tab padding |
| md | 16px | Default element spacing within card sections |
| lg | 24px | Card section padding, space between provider selector and auth tabs |
| xl | 32px | Space between card header and card content |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions: none

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 14px | 600 (semibold) | 1.4 |
| Heading | 24px | 600 (semibold) | 1.2 |
| Caption | 12px | 400 (regular) | 1.5 |

Notes:
- Two weights only: 400 (regular) and 600 (semibold).
- Heading (24px) used for card title "Pi AI Chat" (matches existing `text-2xl`).
- Body (14px) used for descriptions, polling status text, error messages.
- Label (14px/600) used for "Provider", auth method tab labels. Visually distinct from Body at the same 14px size via semibold weight. Maps to existing `text-sm font-semibold` pattern.
- Caption (12px) used for badge text ([OAuth] / [API Key]), secondary helper text.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--background)` oklch(1 0 0) | Page background, card interiors |
| Secondary (30%) | `var(--card)` / `var(--muted)` | Card surfaces, tab bar background, inactive tab fill |
| Accent (10%) | `var(--primary)` oklch(0.205 0 0) | Active tab indicator, primary CTA buttons ("Login with Claude", "Login with Codex", "Go to Chat") |
| Destructive | `var(--destructive)` oklch(0.577 0.245 27.325) | Expired badge, error messages, "Disconnect" button |

### Semantic Badge Colors (token health per D-08)

| State | Color | Tailwind Class | CSS Value |
|-------|-------|----------------|-----------|
| Connected (healthy) | Green | `bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400` | -- |
| Expiring soon (<30 min) | Yellow | `bg-yellow-100 text-yellow-800 dark:bg-yellow-900/30 dark:text-yellow-400` | -- |
| Expired | Red | `bg-red-100 text-red-800 dark:bg-red-900/30 dark:text-red-400` | -- |

### Auth Method Badge Colors

| Badge | Variant | Rationale |
|-------|---------|-----------|
| [OAuth] | `secondary` variant of Badge component | Neutral, non-distracting identifier |
| [API Key] | `secondary` variant of Badge component | Same visual weight as OAuth badge |

Accent reserved for: OAuth login buttons ("Login with Claude", "Login with Codex"), "Go to Chat" primary action button, active auth method tab indicator.

---

## Visual Focal Point

**Disconnected state:** The full-width OAuth login button ("Login with Claude" / "Login with Codex") is the primary visual anchor. It is the only accent-colored element in the card content area and occupies the full width of the tab panel, drawing the eye immediately.

**Connected state:** The full-width "Go to Chat" button is the primary visual anchor, using accent color at full width.

---

## Component Inventory

### New Components Needed

| Component | Source | Notes |
|-----------|--------|-------|
| `Tabs` / `TabsList` / `TabsTrigger` / `TabsContent` | `npx shadcn add tabs` | Auth method selector (OAuth / API Key). D-01 specifies tabs within Card. |

### Existing Components Reused

| Component | File | Usage |
|-----------|------|-------|
| `Card` / `CardHeader` / `CardContent` / `CardTitle` / `CardDescription` | `ui/card.tsx` | Main connection page layout |
| `Button` | `ui/button.tsx` | Login buttons, Connect API Key, Go to Chat, Disconnect, Cancel authorization |
| `Badge` | `ui/badge.tsx` | Auth method badge ([OAuth] / [API Key]) |
| `Input` | `ui/input.tsx` | API Key text input |
| `SegmentedControl` | `connection/segmented-control.tsx` | Provider selector (Anthropic / OpenAI) |

### Icons Used (lucide-react)

| Icon | Usage |
|------|-------|
| `Loader2` | Spinner during OAuth polling ("Waiting for authorization...") and API Key connecting |
| `Check` | Success indicator after connection |
| `Eye` / `EyeOff` | API Key visibility toggle |
| `ExternalLink` | OAuth login button to indicate popup opens |
| `LogOut` | Disconnect button icon |
| `ArrowRight` | "Go to Chat" button icon |

---

## Interaction States

### Connection Page States (per provider)

| State | What User Sees | Key Elements |
|-------|---------------|--------------|
| **Disconnected (initial)** | Provider selector + auth method tabs. OAuth tab active by default (D-02). Login button enabled. | Tabs: OAuth / API Key. OAuth tab shows login button. API Key tab shows input + connect button. |
| **OAuth polling** | Login button replaced with spinner + "Waiting for authorization..." text. "Cancel authorization" link below. Provider selector and tabs disabled. | Loader2 spinning. Text: "Waiting for authorization..." Button variant: secondary/disabled. "Cancel authorization": text link below. |
| **OAuth success** | Connected summary: auth method badge [OAuth] + green health badge. "Go to Chat" and "Disconnect" buttons. No auto-redirect (D-11). | Badge: [OAuth] green. Buttons: "Go to Chat" (primary), "Disconnect" (destructive outline). |
| **OAuth error** | Error message below the login button. Login button re-enabled. | Text: error message from backend. Color: destructive. Button: re-enabled to retry. |
| **API Key connecting** | Connect button shows spinner + "Connecting..." | Same pattern as existing v1.0 connection page. |
| **API Key connected** | Connected summary: auth method badge [API Key] + green badge. "Go to Chat" and "Disconnect" buttons. | Badge: [API Key] green. Same layout as OAuth connected. |
| **API Key error** | Error message below connect button. | Text: error from backend. Color: destructive. |
| **OAuth expiring** | Connected summary with yellow health badge. User can still chat. | Badge: [OAuth] yellow. No action required from user. |
| **OAuth expired** | Connected summary with red health badge. "Re-authenticate" button replaces "Go to Chat" (D-13). | Badge: [OAuth] red. Button: "Re-authenticate" (primary). "Disconnect" still available. |

### Popup Flow (D-04)

| Step | Action |
|------|--------|
| 1 | User clicks "Login with Claude" or "Login with Codex" |
| 2 | Frontend calls `POST /api/auth/oauth/start` with `{ provider }` |
| 3 | On success, `window.open(authUrl, '_blank', 'width=600,height=700,popup=yes')` opens provider consent page |
| 4 | Connection page enters polling state: spinner + "Waiting for authorization..." |
| 5 | Frontend polls `GET /api/auth/oauth/status?provider={p}` every 2000ms |
| 6a | Status `"ok"` -> call `refreshStatus(provider)` to update auth state -> show connected summary |
| 6b | Status `"error"` -> stop polling -> show error message |
| 6c | User clicks "Cancel authorization" -> stop polling -> reset to disconnected |

### Popup Window Spec

| Property | Value |
|----------|-------|
| Width | 600px |
| Height | 700px |
| Features | `popup=yes` (lets browser decide positioning) |
| Fallback | If `window.open` returns `null` (popup blocked), show inline message: "Pop-up blocked. Please allow pop-ups for this site and try again." |

---

## Layout Contract

### Page Layout

```
+----------------------------------------------------------+
|                     (centered, max-w-md)                  |
|  +----------------------------------------------------+  |
|  |  CardHeader                                        |  |
|  |    Title: "Pi AI Chat"           (text-2xl, 600)   |  |
|  |    Desc: "Connect to your LLM provider..."  (14px) |  |
|  +----------------------------------------------------+  |
|  |  CardContent                                       |  |
|  |                                                    |  |
|  |  [Provider Selector: Anthropic | OpenAI]           |  |
|  |                                          (gap: lg) |  |
|  |  [Auth Method Tabs: OAuth | API Key]               |  |
|  |                                          (gap: md) |  |
|  |  +----------------------------------------------+  |  |
|  |  | Tab Content (OAuth or API Key)               |  |  |
|  |  +----------------------------------------------+  |  |
|  |                                                    |  |
|  +----------------------------------------------------+  |
+----------------------------------------------------------+
```

### OAuth Tab Content (disconnected)

```
+----------------------------------------------+
|  [  Login with Claude  ->  ]   (full-width)  |
+----------------------------------------------+
```

### OAuth Tab Content (polling)

```
+----------------------------------------------+
|  [  (spinner)  Waiting for authorization...  ]|
|                                               |
|           Cancel authorization                |
+----------------------------------------------+
```

### API Key Tab Content (disconnected)

```
+----------------------------------------------+
|  Label: "API Key"                             |
|  [ sk-ant-...                          (eye)] |
|                                               |
|  [    Connect API Key    ]    (full-width)    |
+----------------------------------------------+
```

### Connected Summary (any method)

```
+----------------------------------------------+
|  Connected                                    |
|  [OAuth] [Connected]    or   [API Key] [OK]  |
|                                               |
|  [    Go to Chat  ->   ]      (full-width)   |
|  [    Disconnect       ]      (full-width)   |
+----------------------------------------------+
```

### Expired Summary (OAuth only, D-13)

```
+----------------------------------------------+
|  Session Expired                              |
|  [OAuth] [Expired]                            |
|                                               |
|  [  Re-authenticate    ]      (full-width)   |
|  [    Disconnect       ]      (full-width)   |
+----------------------------------------------+
```

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (Anthropic OAuth) | "Login with Claude" |
| Primary CTA (OpenAI OAuth) | "Login with Codex" |
| Primary CTA (API Key) | "Connect API Key" |
| Primary CTA (connected) | "Go to Chat" |
| Tab label: OAuth | "OAuth" |
| Tab label: API Key | "API Key" |
| Card title | "Pi AI Chat" |
| Card description | "Connect to your LLM provider to start chatting" |
| OAuth polling text | "Waiting for authorization..." |
| OAuth cancel link | "Cancel authorization" |
| Connected heading | "Connected" |
| Expired heading | "Session Expired" |
| Auth method badge: OAuth | "OAuth" |
| Auth method badge: API Key | "API Key" |
| Health badge: healthy | "Connected" |
| Health badge: expiring | "Expiring" |
| Health badge: expired | "Expired" |
| Re-authenticate CTA | "Re-authenticate" |
| Disconnect CTA | "Disconnect" |
| Error: popup blocked | "Pop-up blocked. Please allow pop-ups for this site and try again." |
| Error: port conflict (Anthropic) | (from backend) "Port 53692 is already in use..." |
| Error: port conflict (OpenAI) | (from backend) "Port 1455 is already in use..." |
| Error: generic OAuth failure | (from backend, displayed as-is) |
| Error: API Key invalid | "Invalid API key. Check your key and try again." |
| Empty state | N/A -- connection page always shows actionable UI; there is no "empty" data state |
| Destructive: Disconnect | Button label "Disconnect" -- no confirmation dialog. Single click clears credentials for the selected provider. Reversible by re-authenticating. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | tabs, badge, button, card, input, label | not required |

No third-party registries declared (components.json `registries: {}`).

---

## Accessibility Contract

| Concern | Implementation |
|---------|---------------|
| Tab navigation | shadcn Tabs component provides ARIA `role="tablist"`, `role="tab"`, `role="tabpanel"` and keyboard arrow navigation out of the box |
| Badge semantics | Badge elements are decorative (`span`); screen readers get the text content naturally |
| Loading state | OAuth polling button uses `aria-busy="true"` and `aria-disabled="true"` |
| Error announcements | Error messages use `aria-live="polite"` for screen reader announcement |
| Popup window | `rel="noopener"` implied by `window.open` to different origin |
| Focus management | After OAuth completes or errors, focus returns to the primary CTA button |
| Color contrast | Green/yellow/red badges use 800-weight text on 100-weight background (>4.5:1 contrast ratio in light mode) |
| API Key visibility toggle | `Eye` icon button uses `aria-label="Show API key"`; `EyeOff` icon button uses `aria-label="Hide API key"` |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
