---
phase: 07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7
plan: 01
subsystem: auth
tags: [oauth, openai-codex, sse, streaming, error-surfacing, pi-agent-core, message_end, diagnostic]

# Dependency graph
requires:
  - phase: 07-openai-oauth-flow
    provides: "stream-adapter.ts exhaustive switch on ame.type (Plan 07-04), diagnostic log (stream-events-07-04.log), Cause B confirmed"
  - plan: 07-04
    provides: "DEBUG_EVENTS flag, logEvent helper, Cause B empirical confirmation"
provides:
  - "case 'message_end' in stream-adapter.ts emitting SSE event:error when stopReason==='error' && errorMessage present (D-03)"
  - "Diagnostic log .planning/debug/stream-events-07-1-01.log with Outcome 3 classification and handoff to Plan 07.1-02"
affects: [07.1-02-fix-and-reverify, 08-oauth-connection-ui]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "message_end error surfacing: case 'message_end' in AgentEvent switch emits SSE event:error for upstream stream-level errors that pi-agent-core delivers via message_end (not message_update)"
    - "Outcome classification diagnostic: Task 2 classifies SC#3 results into 3 outcomes with decision-tree handoff"

key-files:
  created:
    - .planning/debug/stream-events-07-1-01.log
  modified:
    - src/server/lib/stream-adapter.ts

key-decisions:
  - "D-03 implemented exactly as CONTEXT.md specified: case 'message_end' emits SSE event:error with {message: errorMessage} when stopReason==='error'. Permanent fix (not DEBUG gated). stopReason==='aborted' explicitly no-op."
  - "Outcome 3 (escalation) classified for Task 2: OAuth credentials unavailable in automated worktree context, D-01 escalation to Plan 07.1-02 with live browser consent required."

patterns-established:
  - "AgentEvent message_end handler: new case handles terminal message with error surfacing, covering the gap where message_update is never emitted (Codex upstream behavior)"
  - "Environmental precondition documentation: when automated agent cannot complete browser consent, log is Outcome 3 with explicit handoff steps"

requirements-completed: []  # OAUTH-02 and OAUTH-03 still NOT verified -- SC#3 requires live OAuth test in Plan 07.1-02

# Metrics
duration: 4min
completed: 2026-04-06
---

# Phase 7.1 Plan 01: Error Surfacing Summary

**D-03 case "message_end" SSE error emission added to stream-adapter.ts; SC#3 diagnostic capture classified Outcome 3 (escalation) due to OAuth credentials unavailable in worktree -- handoff to Plan 07.1-02 with full D-02 decision tree**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-06T06:42:05Z
- **Completed:** 2026-04-06T06:46:00Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments

- **D-03 implemented in stream-adapter.ts (Task 1 -- commit d7d7eaa9):** Added new `case "message_end"` branch to the outer `switch (event.type)` in `adaptAgentEvents`. When `event.message.stopReason === "error"` and `event.message.errorMessage` is truthy, emits SSE `event: error` with `data: JSON.stringify({ message: event.message.errorMessage })`. Shape mirrors the existing `case "message_update" -> case "error"` branch. `stopReason === "aborted"` is explicit no-op. Fix is permanent (not gated by DEBUG_EVENTS). TypeScript compiles clean.
- **Diagnostic log created (Task 2 -- commit cf7050a2):** SC#3 curl could not reach stream-adapter code path because OAuth credentials were unavailable (in-memory store empty after worktree backend start). Classified as Outcome 3 (escalation). Documented full D-02 decision tree for all errorMessage patterns in handoff section.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add case "message_end" SSE error emission to stream-adapter.ts (D-03)** - `d7d7eaa9` (feat)
2. **Task 2: Re-run SC#3 UAT curl to capture upstream errorMessage, save to diagnostic log** - `cf7050a2` (docs)

**Plan metadata:** pending (this SUMMARY.md + STATE.md + ROADMAP.md)

## Files Created/Modified

- `src/server/lib/stream-adapter.ts` - Added `case "message_end"` branch (24 lines) between `case "message_update"` and `case "tool_execution_start"`. Emits SSE `event: error` with errorMessage payload when stopReason is "error".
- `.planning/debug/stream-events-07-1-01.log` - SC#3 diagnostic log with Outcome 3 classification, D-02 decision tree handoff, escalation path, and environment notes.

## Code Added (Task 1 Diff)

```typescript
        case "message_end": {
          // Phase 7.1 D-03: surface upstream stream-level errors that pi-agent-core delivers
          // via message_end (when pi-ai throws BEFORE pushing any "start" event -- e.g. Codex
          // HTTP failures from openai-codex-responses.js lines 79/144/159/163/166). Without this
          // branch the errorMessage is silently dropped and the client only sees `event: done`
          // (the gap diagnosed in Phase 7 Plan 07-04).
          if (
            event.message.stopReason === "error" &&
            event.message.errorMessage
          ) {
            void stream.writeSSE({
              event: "error",
              data: JSON.stringify({ message: event.message.errorMessage }),
            })
          }
          break
        }
```

## SC#3 Diagnostic Outcome (Task 2)

**Outcome: 3 (Escalation)**

The D-03 fix is committed and TypeScript-clean but could not be empirically validated because the in-memory credential store has no OAuth token in the automated worktree context (browser consent required). The backend's chat route correctly rejects with HTTP 401 before constructing the Agent, so the stream-adapter code path is never reached.

**Handoff to Plan 07.1-02:** Start backend with D-03 fix active, complete OpenAI OAuth browser consent, re-run SC#3 curl, inspect for event:error with concrete errorMessage. Apply D-02 decision tree based on content.

## Decisions Made

- **D-03 implementation verbatim from CONTEXT.md:** No creative interpretation. The case "message_end" branch matches the exact specification: SSE event:error with `{ message: errorMessage }` payload, permanent (not debug-gated), aborted is no-op.
- **Outcome 3 classification for Task 2:** OAuth credentials are an environmental precondition that cannot be satisfied in an automated worktree agent. The D-03 code is correct and will surface errors when exercised with live credentials in Plan 07.1-02.

## Deviations from Plan

None -- plan executed exactly as written. The Outcome 3 classification was an anticipated possibility per the plan's Task 2 interpretation table (three possible outcomes explicitly documented).

## Issues Encountered

- **tsx not on PATH in worktree:** Resolved by using `./node_modules/.bin/tsx` directly instead of `npx tsx`.
- **OAuth credentials unavailable:** Expected per Phase 05 design (in-memory store, no persistence). Documented as Outcome 3 in diagnostic log with handoff to Plan 07.1-02.

## User Setup Required

None for this plan. Plan 07.1-02 requires live OpenAI OAuth browser consent before SC#3 can be validated.

## Known Stubs

None. The case "message_end" branch is complete production code, not a placeholder. The diagnostic log is an informational artifact, not a code stub.

## Next Phase Readiness

- **D-03 fix is shipped and active:** Any backend start from commit d7d7eaa9+ will have the error surfacing path enabled.
- **Plan 07.1-02 is unblocked:** The fix + diagnostic handoff provide everything needed to run the actual SC#3 test with live OAuth and apply the D-02 decision tree.
- **OAUTH-02 and OAUTH-03 remain open:** Will be closed when SC#3 and SC#4 pass in Plan 07.1-02.

## Self-Check: PASSED

**Files verified:**
- FOUND: `src/server/lib/stream-adapter.ts` (case "message_end" present, DEBUG_EVENTS=false preserved)
- FOUND: `.planning/debug/stream-events-07-1-01.log` (Outcome 3, handoff section, escalation section)
- FOUND: `.planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-01-SUMMARY.md` (this file)

**Commits verified:**
- FOUND: `d7d7eaa9` (Task 1 -- feat: case "message_end" SSE error emission)
- FOUND: `cf7050a2` (Task 2 -- docs: SC#3 diagnostic log)

---
*Phase: 07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7*
*Plan 01: Error Surfacing*
*Completed: 2026-04-06*
