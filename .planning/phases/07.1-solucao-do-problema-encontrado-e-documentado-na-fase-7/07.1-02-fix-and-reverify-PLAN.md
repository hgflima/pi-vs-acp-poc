---
phase: 07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7
plan: 02
type: execute
wave: 2
depends_on: ["07.1-01"]
files_modified:
  - src/server/agent/setup.ts
  - src/server/routes/chat.ts
  - src/server/lib/credentials.ts
  - src/server/routes/oauth.ts
  - patches/
  - package.json
  - .planning/phases/07-openai-oauth-flow/07-UAT.md
  - .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - OAUTH-02
  - OAUTH-03

must_haves:
  truths:
    - "POST /api/chat with provider=openai and model=gpt-5.1 (Codex) produces an SSE stream with ≥1 event: text_delta containing real assistant content"
    - "POST /api/auth/oauth/debug/force-expire?provider=openai followed by POST /api/chat completes successfully, and GET /api/auth/status?provider=openai returns an oauthExpiry newer than the forced-past value"
    - "OAUTH-02 marked [x] Complete in REQUIREMENTS.md traceability table"
    - "OAUTH-03 marked [x] Complete in REQUIREMENTS.md traceability table"
    - "Phase 7 VERIFICATION.md frontmatter status changes from gaps_found to verified"
    - "07-UAT.md has a new ## Re-verification (Phase 7.1) section documenting post-fix SC#3 PASS and SC#4 PASS"
    - "No gambiarra — the fix is either a targeted src/server change, a patch-package-generated patch, or both; bypass adapter (custom Codex handler in our repo) was NOT used"
  artifacts:
    - path: ".planning/phases/07-openai-oauth-flow/07-UAT.md"
      provides: "Re-verification (Phase 7.1) section appended after Re-verification (Phase 7.1 gap closure) section"
      contains: "## Re-verification (Phase 7.1)"
    - path: ".planning/phases/07-openai-oauth-flow/07-VERIFICATION.md"
      provides: "frontmatter status: verified"
      contains: "status: verified"
    - path: ".planning/REQUIREMENTS.md"
      provides: "OAUTH-02 and OAUTH-03 both marked [x] Complete"
      contains: "OAUTH-02"
  key_links:
    - from: "fix applied (src/server/ OR patches/)"
      to: "pi-ai openai-codex-responses.js streamOpenAICodexResponses"
      via: "either our src/server/ resolves the mis-configured input (header/credential/body) OR patch-package patches pi-ai directly"
      pattern: "depends on D-02 branch"
    - from: "07-UAT.md"
      to: "REQUIREMENTS.md + ROADMAP.md + 07-VERIFICATION.md"
      via: "closure propagation — UAT PASS triggers four document updates atomically"
      pattern: "## Re-verification (Phase 7.1)"
---

<objective>
Apply the appropriate fix based on Plan 07.1-01's diagnostic handoff (following D-02's decision tree), then re-verify SC#3 and SC#4 end-to-end via manual UAT curl per D-04's closure criterion, then propagate the PASS outcome across the four status documents (07-UAT.md, 07-VERIFICATION.md, REQUIREMENTS.md, ROADMAP.md).

Purpose: Plan 07.1-01 surfaced the upstream errorMessage by landing D-03 (case "message_end" in stream-adapter.ts). This plan closes the actual gap — applying engineering-adequate fix (NOT gambiarra) to make OAuth Codex chat work, then proving both SC#3 (text_delta events arrive with assistant content) and SC#4 (force-expire + chat triggers auto-refresh mutex) via live curl with real OAuth credentials.

Output: Working OAuth Codex chat stream with auto-refresh verified; OAUTH-02 and OAUTH-03 closed; Phase 7 overall status moved from BLOCKED to Complete.

**Autonomous: false** — this plan's path is DIAGNOSTIC-GATED by 07.1-01's output. The first task is a human-verify checkpoint that triages the diagnostic log and selects the D-02 branch. Subsequent tasks also include a human-verify checkpoint for SC#3/SC#4 UAT execution (per D-08 Phase 7 convention: manual curl with real browser consent, not mock).
</objective>

<execution_context>
@/Users/henriquelima/Documents/dev/personal/pi-ai-poc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henriquelima/Documents/dev/personal/pi-ai-poc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-CONTEXT.md
@.planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-01-error-surfacing-PLAN.md
@.planning/debug/stream-events-07-1-01.log
@.planning/phases/07-openai-oauth-flow/07-VERIFICATION.md
@.planning/phases/07-openai-oauth-flow/07-04-SUMMARY.md
@.planning/phases/07-openai-oauth-flow/07-UAT.md
@.planning/phases/07-openai-oauth-flow/07-CONTEXT.md
@src/server/lib/stream-adapter.ts
@src/server/agent/setup.ts
@src/server/lib/credentials.ts
@src/server/routes/oauth.ts
@src/server/routes/chat.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Triage 07.1-01 diagnostic log and select D-02 fix branch</name>
  <files>.planning/debug/stream-events-07-1-01.log (read-only input to decision)</files>
  <action>
    This is a checkpoint:decision task. The human reads the diagnostic log from Plan 07.1-01 Task 2, maps the extracted errorMessage (or Outcome classification) to one of D-02's five branches (branch-a, branch-b, branch-c, blocked-escalation, outcome-1-skip), and declares the selection via the resume-signal. See the &lt;decision&gt;, &lt;context&gt;, &lt;options&gt;, and &lt;resume-signal&gt; blocks below for the full triage contract. The branch selection determines Task 2's action scope. Record the selection in a brief comment in STATE.md or inline in this plan file so Task 2 can pick it up unambiguously.
  </action>
  <verify>
    <automated>test -f .planning/debug/stream-events-07-1-01.log &amp;&amp; grep -E '^(Outcome: [123]|## Handoff to Plan 07.1-02)' .planning/debug/stream-events-07-1-01.log</automated>
  </verify>
  <done>Human has declared one of: branch-a, branch-b, branch-c, blocked-escalation, outcome-1-skip. For branch-a/b/c the concrete fix (file + change summary) is named. Decision is recorded so Task 2 knows which branch to implement.</done>


  <read_first>
    - .planning/debug/stream-events-07-1-01.log (the handoff artifact from Plan 07.1-01 Task 2 — MUST exist, must have Outcome 1/2/3 classification and Handoff section)
    - .planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-CONTEXT.md D-02 section (lines 19-23) — the decision tree verbatim
    - node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js lines 56-184 (throw points at 79, 144, 159, 163, 166)
    - .planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-CONTEXT.md D-01 escalation section (lines 17) — if Outcome 3, escalation is required
  </read_first>

  <decision>
    Given Plan 07.1-01's diagnostic output (captured in .planning/debug/stream-events-07-1-01.log), select the appropriate fix strategy per D-02's decision tree. The captured errorMessage content dictates the branch.
  </decision>

  <context>
    D-02 is deliberately deferred by CONTEXT.md until the diagnostic is in hand. The locked principle is "sem gambiarras" — bypass adapter (custom Codex handler in our repo) is last resort and contradicts the POC's validation objective. Patch-package is PREFERRED over bypass for pi-ai fixes.

    Three possible fix branches per D-02:
    - **Branch A (our code):** errorMessage points to a header we send wrong, an OAuth scope missing in our /start call, a body shape mismatch, or a credential resolution bug in resolveCredential/setup.ts. Fix lives in src/server/, no pi-ai changes.
    - **Branch B (patch-package, trivial):** errorMessage points to a pi-ai bug with clear minimal fix — missing header (e.g. `OpenAI-Beta`), wrong transport flag, incorrect `originator`, wrong path in request URL. Fix via `npx patch-package @mariozechner/pi-ai` after editing node_modules, commits `patches/@mariozechner+pi-ai+0.64.x.patch`.
    - **Branch C (patch-package, structural):** errorMessage reveals deeper divergence (API endpoint changed, ChatGPT backend shape drift, stream parsing broken). Still prefer patch-package over bypass adapter — may be heavier patch but preserves pi-ai integration. Bypass adapter is ONLY if patch is impossible.

    If the diagnostic log shows Outcome 1 (SC#3 already passes from Task 1's D-03 — unlikely but possible), skip directly to Task 2 re-verification with note "fix was D-03 itself".

    If the diagnostic log shows Outcome 3 (errorMessage insufficient, escalation required), this task REJECTS the decision and blocks on Plan 07.1-01 re-execution with D-01 escalation (curl -v replication OR pi-ai throw-point logging).
  </context>

  <options>
    <option id="branch-a">
      <name>Branch A: Fix in src/server/ (our code)</name>
      <pros>No node_modules mutation; stays within POC codebase; fast CI; no patch-package dependency</pros>
      <cons>Only viable if errorMessage actually points to our code (header, scope, body, credential resolution)</cons>
      <applies-when>errorMessage contains "No API key" / "unauthorized" + our OAuth flow is correct / body shape mismatch traceable to our serialize calls</applies-when>
    </option>

    <option id="branch-b">
      <name>Branch B: patch-package on pi-ai (trivial patch)</name>
      <pros>Preserves pi-ai stack validation objective; revisable patch committed; auto-applied on npm install</pros>
      <cons>Adds patch-package dev dependency; patch must be maintained across pi-ai version bumps</cons>
      <applies-when>errorMessage reveals clear minimal pi-ai bug — missing header, wrong flag, incorrect originator, wrong transport</applies-when>
    </option>

    <option id="branch-c">
      <name>Branch C: patch-package on pi-ai (structural patch)</name>
      <pros>Preserves pi-ai stack; reviewable; still upstreamable later</pros>
      <cons>Larger patch surface; may need re-patching on every pi-ai minor bump</cons>
      <applies-when>errorMessage reveals API endpoint change, stream parsing broken, or ChatGPT backend shape drift</applies-when>
    </option>

    <option id="blocked-escalation">
      <name>BLOCKED — requires Plan 07.1-01 D-01 escalation</name>
      <pros>Honors "no gambiarra" — do not guess the fix</pros>
      <cons>Delays closure; needs additional diagnostic round (curl -v against chatgpt.com/backend-api OR logging in pi-ai throw points)</cons>
      <applies-when>07.1-01 diagnostic log shows Outcome 3 (errorMessage insufficient or absent)</applies-when>
    </option>

    <option id="outcome-1-skip">
      <name>Outcome 1 — D-03 itself was curative, skip to re-verification</name>
      <pros>Fastest path — no code changes needed beyond D-03</pros>
      <cons>Needs explicit verification that SC#3 actually passes (the 07.1-01 log must show event: text_delta)</cons>
      <applies-when>07.1-01 diagnostic log shows Outcome 1 (text_delta events captured post-D-03)</applies-when>
    </option>
  </options>

  <resume-signal>
    Select: branch-a, branch-b, branch-c, blocked-escalation, or outcome-1-skip.

    If branch-a/b/c: name the specific change (file path and one-line summary) before proceeding to Task 2.
    If blocked-escalation: this plan pauses; return to Plan 07.1-01 and run D-01 escalation (curl -v replication OR pi-ai throw-point logging).
    If outcome-1-skip: jump directly to Task 3 (re-verify SC#3/SC#4).
  </resume-signal>

  <acceptance_criteria>
    - User has explicitly stated one of: `branch-a`, `branch-b`, `branch-c`, `blocked-escalation`, `outcome-1-skip`
    - For branch-a/b/c: user has named the concrete fix (file + change summary)
    - For blocked-escalation: this plan's execution is paused and Plan 07.1-01 is re-queued with escalation scope
    - Decision is recorded in STATE.md or a plan comment so downstream Task 2 knows which branch it is implementing
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply selected D-02 fix branch (our code / patch-package trivial / patch-package structural)</name>
  <files>depends on Task 1 decision — see below</files>

  <read_first>
    - .planning/debug/stream-events-07-1-01.log (the concrete errorMessage being addressed)
    - The specific file targeted by the Task 1 decision:
      - Branch A: src/server/agent/setup.ts (createAgent, resolveCredential) AND src/server/lib/credentials.ts AND src/server/routes/oauth.ts AND src/server/routes/chat.ts — READ ALL FOUR to understand OAuth credential resolution, body shape of POST /api/chat, and what headers/scope our server sends
      - Branch B or C: node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js lines 56-220 (full IIFE + buildSSEHeaders at 88 + buildRequestBody at 200)
    - node_modules/@mariozechner/pi-ai/package.json (for exact version to patch — field "version", currently likely 0.64.x)
    - .planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-CONTEXT.md D-02 section verbatim
    - .planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-CONTEXT.md <specifics> section ("Sem gambiarras") — the guardrail
  </read_first>

  <action>
    Apply the fix selected in Task 1. **The exact actions depend on the branch** — implement ONLY the selected branch:

    ---
    **If Branch A (our code fix):**

    Edit the specific src/server/ file identified during Task 1. Example shapes (not prescriptive — actual edit depends on errorMessage content):
    - If errorMessage indicates "No API key for provider: openai-codex": investigate `resolveCredential` in src/server/agent/setup.ts — the async getApiKey callback may be returning undefined/empty for openai-codex because the internal Provider remap (resolvePiProvider) returns "openai-codex" but the credential store still keys by original "openai" Provider. Verify that getActiveCredential("openai") is called with the original Provider slug (not the remapped "openai-codex") inside the callback.
    - If errorMessage indicates 401/403/unauthorized: verify OAuth scopes sent in POST /api/auth/oauth/start handler in src/server/routes/oauth.ts (pi-ai's loginOpenAICodex may need additional scope beyond `openid profile email offline_access` — check pi-ai's loginOpenAICodex source for required scopes).
    - If errorMessage indicates body shape mismatch: verify POST /api/chat handler in src/server/routes/chat.ts is passing message context in the format pi-agent-core Agent expects (not the raw /api/chat body shape).
    - If errorMessage indicates originator/session mismatch: verify `sessionId` option passed to pi-ai streaming functions (if we pass one at all — may need to start passing it).

    Do NOT modify files beyond what the errorMessage dictates. Do NOT "add defensive code" elsewhere opportunistically. Surgical fix only.

    ---
    **If Branch B or C (patch-package on pi-ai):**

    Step 1 — Install patch-package as a devDependency (skip if already installed):
    ```bash
    npm install --save-dev patch-package postinstall-postinstall
    ```

    Step 2 — Add `postinstall` script to package.json (skip if already set):
    ```json
    "scripts": {
      "postinstall": "patch-package"
    }
    ```

    Step 3 — Edit `node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js` (the compiled JS, which is what Node.js actually runs; source .ts in pi-mono is not shipped in the installed package). Apply the minimal change that resolves the errorMessage. Examples (not prescriptive):
    - If missing header: add to `buildSSEHeaders` (line 88) return object: `"OpenAI-Beta": "..."` or whatever header the errorMessage points to.
    - If wrong originator: change the `originator` value in buildRequestBody (line 200) or buildSSEHeaders.
    - If endpoint path wrong: change `resolveCodexUrl` (referenced at line 123).

    Keep the patch as SMALL as possible. Do NOT refactor pi-ai code. Do NOT rename variables. Add a 1-line comment `// Phase 7.1 POC patch — see .planning/phases/07.1-*/07.1-02-SUMMARY.md` above each changed line.

    Step 4 — Generate the patch file:
    ```bash
    npx patch-package @mariozechner/pi-ai
    ```
    This creates `patches/@mariozechner+pi-ai+<version>.patch` automatically.

    Step 5 — Verify the patch applies cleanly on a fresh install:
    ```bash
    # Revert the direct edit to prove the patch is the source of truth
    git checkout -- node_modules/@mariozechner/pi-ai 2>/dev/null || true
    rm -rf node_modules/@mariozechner/pi-ai
    npm install
    # Confirm the patch was applied during postinstall (look for patch-package output line)
    ```

    Alternatively, verify more directly by reading the patched file after reinstall:
    ```bash
    grep -c "Phase 7.1 POC patch" node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js
    # Should return ≥ 1
    ```

    ---
    **If Outcome 1 (D-03 was curative — skip):** No code changes here, proceed directly to Task 3.

    ---
    **Guardrails for ALL branches:**
    - Do NOT create any bypass-adapter file (e.g., src/server/lib/codex-stream-handler.ts or similar). CONTEXT.md specifies bypass is last resort; none of the three defined branches produce a bypass.
    - Do NOT touch src/server/lib/stream-adapter.ts (D-03 already landed in Plan 07.1-01 — this plan does not modify it).
    - Do NOT cleanup the `port 53692` comment in src/server/routes/oauth.ts:60 unless you are already editing that file for Branch A. (It is deferred as oportunístico per CONTEXT.md <deferred>.)

    Record exactly what you changed in Task 3's UAT re-verification section.
  </action>

  <verify>
    <automated>
    # Branch-conditional verification. Exactly ONE of these must pass.
    # Branch A: at least one file in src/server/ changed since 07.1-01
    # Branch B/C: patches/ directory contains pi-ai patch file AND package.json has postinstall script
    # Outcome 1: no source change in this task (skip verification, Task 3 proves correctness)
    (git diff --name-only HEAD src/server/ | grep -v stream-adapter.ts | head -1) || (test -f patches/@mariozechner+pi-ai+*.patch && grep -q '"postinstall"' package.json) || echo "OUTCOME_1_NO_CHANGE"
    </automated>
  </verify>

  <acceptance_criteria>
    - Exactly ONE of the following holds (matching Task 1 decision):
      - **Branch A**: `git diff --name-only HEAD src/server/` shows at least one modified file (not stream-adapter.ts), AND `npx tsc --noEmit` exits 0.
      - **Branch B/C**: `patches/` directory contains at least one file matching `@mariozechner+pi-ai+*.patch` (verify with `ls patches/@mariozechner+pi-ai+*.patch`), AND `grep '"postinstall"' package.json` returns `"postinstall": "patch-package"`, AND `grep -c "Phase 7.1 POC patch" node_modules/@mariozechner/pi-ai/dist/providers/openai-codex-responses.js` returns ≥ 1.
      - **Outcome 1**: no changes this task — `git status --short` shows no modifications beyond Plan 07.1-01's artifacts.
    - NO new files under `src/server/` named `codex-*`, `bypass-*`, or similar (anti-bypass guardrail).
    - NO changes to `src/server/lib/stream-adapter.ts` in this task's diff (D-03 already landed in 07.1-01).
    - If Branch A: the changed file(s) match what was stated in Task 1 resume signal (user-named files).
    - If Branch B/C: the patch is minimal (diff < ~50 lines in the .patch file for trivial fixes; structural may be larger but should not rewrite functions).
  </acceptance_criteria>

  <done>
    The selected D-02 fix branch is applied exactly as decided in Task 1. Either: a surgical src/server/ edit addresses the errorMessage (Branch A), OR a patch-package-generated patch is committed and the post-install hook verifies it applies cleanly (Branch B/C), OR no change (Outcome 1). No bypass adapter introduced. No unrelated files touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-verify SC#3 and SC#4 via manual curl UAT (per D-04 closure criterion)</name>
  <files>.planning/phases/07-openai-oauth-flow/07-UAT.md</files>
  <action>
    This is a checkpoint:human-verify task. The human runs the SC#3 and SC#4 curl commands against a live backend with real OAuth Codex credentials (per D-08 Phase 7 convention), captures SSE output, and verifies the acceptance criteria before proceeding. The concrete commands, interpretation rules, and UAT.md append instructions are in the &lt;how-to-verify&gt; block below. After running, the human appends a new "## Re-verification (Phase 7.1 — final closure)" section to 07-UAT.md (preserving the existing 07-04 "## Re-verification (Phase 7.1 gap closure)" section) and, if both SC#3 and SC#4 PASS, updates the 07-UAT.md frontmatter from `status: FAIL` to `status: PASS`. If either fails, the human reports the failure mode via the resume-signal and this plan pauses for re-triage.
  </action>
  <verify>
    <automated>grep -c '## Re-verification (Phase 7.1 — final closure)' .planning/phases/07-openai-oauth-flow/07-UAT.md &amp;&amp; grep -c '## Re-verification (Phase 7.1 gap closure)' .planning/phases/07-openai-oauth-flow/07-UAT.md &amp;&amp; test -f /tmp/sc3-rerun-07-1-02.sse</automated>
  </verify>
  <done>07-UAT.md contains the new "## Re-verification (Phase 7.1 — final closure)" section with SC#3 result (PASS/FAIL), SC#4 result (PASS/FAIL), updated final outcome, and phase status. If both PASS: 07-UAT.md frontmatter `status: PASS`. Human has responded with "approved" / "partial" / "failed" via resume-signal. SC#3 re-run output saved to /tmp/sc3-rerun-07-1-02.sse.</done>


  <read_first>
    - .planning/phases/07-openai-oauth-flow/07-UAT.md Test 3 (SC#3, lines 102-160) and Test 4 (SC#4, lines 163-167) — re-use the exact curl commands (update paths only if needed)
    - .planning/phases/07-openai-oauth-flow/07-CONTEXT.md D-06 (force-expire endpoint) and D-08 (UAT via curl)
    - .planning/phases/07-openai-oauth-flow/07-04-SUMMARY.md "Deviations from Plan" — notes environmental issues (tsx watch loses OAuth state, port 1455 stale listener). Handle the same way if they recur.
    - .planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-CONTEXT.md D-04 (closure criterion) verbatim
    - src/server/lib/stream-adapter.ts (confirm Plan 07.1-01's case "message_end" branch is live before running)
    - src/server/routes/oauth.ts (confirm POST /debug/force-expire is wired)
    - .planning/phases/07-openai-oauth-flow/07-UAT.md entire file — you will append a new section after the existing "## Re-verification (Phase 7.1 gap closure)" section at line 227. Do NOT overwrite that section.
  </read_first>

  <what-built>
    Task 1 triage + Task 2 fix (whichever branch was selected). Now running manual UAT curl to verify SC#3 (OAuth Codex chat produces assistant content) and SC#4 (force-expire triggers auto-refresh mid-request) per D-04's closure criterion — the ONLY criterion that closes Phase 7.1.
  </what-built>

  <how-to-verify>
    **Prerequisites:**
    1. Backend is running: if not, `npm run dev:backend` (starts port 3001).
    2. OAuth credential present: `curl -sS http://localhost:3001/api/auth/status?provider=openai | jq` must show `"hasOAuth": true` and `"activeMethod": "oauth"`. If not (in-memory store lost on restart), run fresh OAuth:
       ```bash
       curl -sS -X POST http://localhost:3001/api/auth/oauth/start \
         -H "Content-Type: application/json" \
         -d '{"provider":"openai"}' | jq .
       # Open the returned authUrl in a browser, complete consent.
       ```

    **SC#3 verification — OAuth Codex chat produces assistant content:**
    ```bash
    curl -sS -N -X POST http://localhost:3001/api/chat \
      -H "Content-Type: application/json" \
      -d '{"provider":"openai","model":"gpt-5.1","message":"Say OK and nothing else"}' \
      | tee /tmp/sc3-rerun-07-1-02.sse
    ```
    Expected: SSE output contains ≥1 line starting with `event: text_delta` with a `data: {"data":"..."}` payload whose concatenated content includes "OK" (or the model's response to the prompt). Final line is `event: done`. NO `event: error` should appear (unless model legitimately refuses, which is itself a valid signal that OAuth + plumbing work).

    Count text_delta events:
    ```bash
    grep -c '^event: text_delta$' /tmp/sc3-rerun-07-1-02.sse
    # Must return ≥ 1
    ```

    **SC#4 verification — force-expire + chat triggers auto-refresh:**
    ```bash
    # Step 1: capture current oauthExpiry
    curl -sS http://localhost:3001/api/auth/status?provider=openai | jq -r '.oauthExpiry' > /tmp/expiry-before.txt

    # Step 2: force-expire
    curl -sS -X POST "http://localhost:3001/api/auth/oauth/debug/force-expire?provider=openai" | jq .
    # Expected: { "ok": true, ... } with expiry set to a past value

    # Step 3: send chat that must trigger auto-refresh mid-flight
    curl -sS -N -X POST http://localhost:3001/api/chat \
      -H "Content-Type: application/json" \
      -d '{"provider":"openai","model":"gpt-5.1","message":"Say OK and nothing else"}' \
      | tee /tmp/sc4-rerun-07-1-02.sse

    # Step 4: confirm oauthExpiry advanced past the force-expired value
    curl -sS http://localhost:3001/api/auth/status?provider=openai | jq -r '.oauthExpiry' > /tmp/expiry-after.txt

    # Expected: expiry-after.txt contains a larger Unix ms timestamp than expiry-before.txt
    # AND sc4-rerun-07-1-02.sse contains ≥1 event: text_delta (chat completed successfully)
    BEFORE=$(cat /tmp/expiry-before.txt)
    AFTER=$(cat /tmp/expiry-after.txt)
    echo "before=$BEFORE after=$AFTER pass=$([[ $AFTER -gt $BEFORE ]] && echo YES || echo NO)"
    ```
    Expected: `pass=YES` — oauthExpiry advanced, proving the refresh mutex fired during the chat request.

    **If SC#3 still FAILs (no text_delta):** Do NOT mark PASS. Return to Plan 07.1-02 Task 1 and re-triage — the Task 2 fix was incorrect. Document the residual symptom in 07-UAT.md and halt.

    **If SC#3 PASSes but SC#4 FAILs:** the auto-refresh path has an independent bug (separate from SC#3 gap). Document it and decide whether to fix in this plan or escalate to a follow-up plan. DO NOT mark OAUTH-03 complete in that case.

    **Append a new section to 07-UAT.md** (MUST append, MUST NOT overwrite the existing "## Re-verification (Phase 7.1 gap closure)" section at line 227). Insert AFTER that section ends (after the horizontal rule at the end). Format:

    ```markdown
    ## Re-verification (Phase 7.1 — final closure)

    **Phase 7.1 plans:** 07.1-01-error-surfacing-PLAN.md, 07.1-02-fix-and-reverify-PLAN.md
    **Re-verified:** <ISO timestamp>
    **Fix applied:** <Branch A / B / C / Outcome-1 — describe in 1 sentence what was changed>
    **Root cause (extracted from .planning/debug/stream-events-07-1-01.log):** <the errorMessage and which openai-codex-responses.js throw point it pointed to>

    ### SC#3 Re-run — Chat stream with OAuth token (Codex model)
    curl: <same as above>
    Actual (SSE output): <paste first 20 lines of /tmp/sc3-rerun-07-1-02.sse>
    text_delta event count: <N>
    Result: PASS / FAIL

    ### SC#4 Re-run — Auto-refresh via force-expire + chat
    Before expiry: <timestamp>
    Force-expire response: <JSON>
    Chat result: PASS / FAIL (≥1 text_delta)
    After expiry: <timestamp>
    Advanced: YES / NO
    Result: PASS / FAIL

    ### Updated Final Outcome
    Overall (post-Phase-7.1-final-closure): <PASS / FAIL>
    - SC#1: PASS (unchanged)
    - SC#2: PASS (unchanged)
    - SC#3: <PASS / FAIL>
    - SC#4: <PASS / FAIL>
    - SC#5: PASS (unchanged)

    Phase 7 status: <Complete / STILL BLOCKED>
    OAUTH-02: <Complete / STILL INCOMPLETE>
    OAUTH-03: <Complete / STILL INCOMPLETE>

    Re-verified by: <name>
    Re-verified at: <ISO timestamp>
    ```

    **If overall outcome is PASS**, also update 07-UAT.md frontmatter: change `status: FAIL` → `status: PASS`.
  </how-to-verify>

  <resume-signal>
    Type "approved" if SC#3 PASS AND SC#4 PASS (both green, Phase 7.1 closure criterion D-04 met).
    Type "partial" if SC#3 PASS but SC#4 FAIL (report details).
    Type "failed" with description if SC#3 still FAIL (Task 2 fix did not resolve the gap — triage loop).
  </resume-signal>

  <acceptance_criteria>
    - File `.planning/phases/07-openai-oauth-flow/07-UAT.md` contains `## Re-verification (Phase 7.1 — final closure)` section (verify: `grep -c '## Re-verification (Phase 7.1 — final closure)' .planning/phases/07-openai-oauth-flow/07-UAT.md` returns 1).
    - Existing `## Re-verification (Phase 7.1 gap closure)` section from Plan 07-04 is PRESERVED (verify: `grep -c '## Re-verification (Phase 7.1 gap closure)' .planning/phases/07-openai-oauth-flow/07-UAT.md` returns 1).
    - SC#3 re-run output saved to `/tmp/sc3-rerun-07-1-02.sse` AND `grep -c '^event: text_delta$' /tmp/sc3-rerun-07-1-02.sse` returns ≥ 1 (for PASS).
    - SC#4 before/after expiry comparison returns `pass=YES` (after-expiry timestamp > before-expiry timestamp).
    - If SC#3 PASS + SC#4 PASS: 07-UAT.md frontmatter line `status: FAIL` is changed to `status: PASS` (verify: `grep -E '^status: PASS' .planning/phases/07-openai-oauth-flow/07-UAT.md` returns 1 match in the frontmatter block).
    - User has typed "approved" / "partial" / "failed" at the checkpoint.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Propagate PASS outcome across REQUIREMENTS, VERIFICATION, ROADMAP (D-04 closure artifacts)</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md,
    .planning/ROADMAP.md
  </files>

  <read_first>
    - .planning/REQUIREMENTS.md (entire file, 65 lines) — find OAUTH-02 and OAUTH-03 lines + Traceability table rows
    - .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md (frontmatter only, lines 1-27) — status field and gaps array
    - .planning/ROADMAP.md lines 60-75 (Phase 7 row) and lines 102-112 (Progress table) — status column for Phase 7
    - .planning/phases/07-openai-oauth-flow/07-UAT.md Re-verification (Phase 7.1 — final closure) section (created in Task 3) — source of truth for outcome
  </read_first>

  <action>
    **Only execute this task if Task 3 resume signal was "approved" (SC#3 PASS AND SC#4 PASS).** If "partial" or "failed", STOP and do not propagate — document the partial state in STATE.md instead and halt.

    **Update 1 — REQUIREMENTS.md:**
    - Change line 18 `- [ ] **OAUTH-02**: ...` → `- [x] **OAUTH-02**: ...`
    - Change line 19 `- [ ] **OAUTH-03**: ...` → `- [x] **OAUTH-03**: ...`
    - In the Traceability table (lines 45-55), update:
      - `| OAUTH-02 | Phase 7 | In Progress (UAT FAIL — SC#3 gap) |` → `| OAUTH-02 | Phase 7 | Complete |`
      - `| OAUTH-03 | Phase 7 | In Progress (UAT SKIPPED — depends on SC#3) |` → `| OAUTH-03 | Phase 7 | Complete |`
    - Update the footer `*Last updated: 2026-04-04 ...*` line to reflect today's date and the Phase 7.1 closure: `*Last updated: <YYYY-MM-DD> after Phase 7.1 closure — OAUTH-02 and OAUTH-03 completed via D-03 error-surfacing + D-02 fix*`

    **Update 2 — 07-VERIFICATION.md:**
    - Change frontmatter line 4 `status: gaps_found` → `status: verified`
    - Change frontmatter line 5 `score: 4/5 success criteria verified` → `score: 5/5 success criteria verified`
    - Change frontmatter line 6 `re_verification: false` → `re_verification: true`
    - In the `gaps:` array (lines 7-27), change both gap entries' `status: failed` → `status: resolved`
    - Append a new bottom-of-file section after line 157:
      ```markdown

      ## Phase 7.1 Closure (re-verified <YYYY-MM-DD>)

      Both gaps resolved. SC#3 and SC#4 now PASS per 07-UAT.md "Re-verification (Phase 7.1 — final closure)" section.

      **Fix applied:** <1-sentence summary of Branch A/B/C/Outcome-1 fix from 07.1-02 Task 2>
      **Root cause:** <from 07.1-01 diagnostic log>

      **Re-verified by:** <name>
      **Re-verified at:** <ISO timestamp>
      ```

    **Update 3 — ROADMAP.md:**
    - Line 28 (Phase 7 bullet): `- [ ] **Phase 7: OpenAI OAuth Flow** - ...` → `- [x] **Phase 7: OpenAI OAuth Flow** - Backend OAuth routes for OpenAI Codex with PKCE, token storage, and automatic refresh (completed <YYYY-MM-DD>)`
    - Line 70 (Plans header): `**Plans**: 4 plans (includes Phase 7.1 gap closure)` → `**Plans**: 4 plans (includes Phase 7.1 gap closure) — COMPLETE`
    - Phase 7.1 row (lines 76-84): change `**Goal:** [Urgent work - to be planned]` → `**Goal:** Close SC#3 and SC#4 for Phase 7 (OAUTH-02/OAUTH-03) via D-03 error-surfacing + D-02 diagnostic-gated fix`. Change `**Plans:** 0 plans` → `**Plans:** 2 plans`. Replace `- [ ] TBD (run /gsd:plan-phase 07.1 to break down)` with:
      ```
      - [x] 07.1-01-error-surfacing-PLAN.md — D-03 case "message_end" in stream-adapter.ts + diagnostic capture
      - [x] 07.1-02-fix-and-reverify-PLAN.md — Apply D-02 fix (Branch <A/B/C>) + re-verify SC#3 + SC#4 + propagate closure
      ```
    - In the Progress table (lines 102-112), update Phase 7 row: `| 7. OpenAI OAuth Flow | v1.1 | 3/4 | Gap Closure (...) | - |` → `| 7. OpenAI OAuth Flow | v1.1 | 4/4 | Complete | <YYYY-MM-DD> |`
    - Add a new row for Phase 7.1 after Phase 7 row: `| 7.1. Solução do Problema (Phase 7 Gap Closure) | v1.1 | 2/2 | Complete | <YYYY-MM-DD> |`

    Use `date -u +%Y-%m-%d` to fetch the current date for all `<YYYY-MM-DD>` placeholders. Use `date -u +%Y-%m-%dT%H:%M:%SZ` for ISO timestamps.

    Do NOT modify any other sections of REQUIREMENTS.md / 07-VERIFICATION.md / ROADMAP.md beyond those specified.
  </action>

  <verify>
    <automated>grep -c '^\- \[x\] \*\*OAUTH-02\*\*' .planning/REQUIREMENTS.md && grep -c '^\- \[x\] \*\*OAUTH-03\*\*' .planning/REQUIREMENTS.md && grep -c '^status: verified' .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md && grep -c '4/4 | Complete' .planning/ROADMAP.md && grep -c '7.1-01-error-surfacing-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - REQUIREMENTS.md: `grep -c '\- \[x\] \*\*OAUTH-02\*\*' .planning/REQUIREMENTS.md` returns `1`.
    - REQUIREMENTS.md: `grep -c '\- \[x\] \*\*OAUTH-03\*\*' .planning/REQUIREMENTS.md` returns `1`.
    - REQUIREMENTS.md: `grep 'OAUTH-02 | Phase 7 | Complete' .planning/REQUIREMENTS.md` returns a match (traceability row updated).
    - REQUIREMENTS.md: `grep 'OAUTH-03 | Phase 7 | Complete' .planning/REQUIREMENTS.md` returns a match.
    - REQUIREMENTS.md: `grep -c 'In Progress' .planning/REQUIREMENTS.md` for OAUTH-02/OAUTH-03 rows returns `0`.
    - 07-VERIFICATION.md: `grep -c '^status: verified' .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md` returns `1` (frontmatter updated).
    - 07-VERIFICATION.md: `grep -c '^status: gaps_found' .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md` returns `0`.
    - 07-VERIFICATION.md: file contains `## Phase 7.1 Closure` section.
    - 07-VERIFICATION.md: both gap entries in frontmatter gaps array show `status: resolved` (`grep -c 'status: resolved' .planning/phases/07-openai-oauth-flow/07-VERIFICATION.md` returns `≥ 2`).
    - ROADMAP.md: `grep -c '4/4 | Complete' .planning/ROADMAP.md` returns `≥ 1` (Phase 7 row completion).
    - ROADMAP.md: `grep -c '07.1-01-error-surfacing-PLAN.md' .planning/ROADMAP.md` returns `≥ 1` and `grep -c '07.1-02-fix-and-reverify-PLAN.md' .planning/ROADMAP.md` returns `≥ 1` (Phase 7.1 plans listed).
    - ROADMAP.md: `grep -c 'TBD (run /gsd:plan-phase 07.1' .planning/ROADMAP.md` returns `0` (placeholder removed).
    - ROADMAP.md: Phase 7.1 Progress table row exists with `2/2 | Complete`.
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md marks OAUTH-02 and OAUTH-03 as [x] Complete in both the active requirements list and the traceability table. 07-VERIFICATION.md frontmatter status is `verified` with both gaps resolved and a `## Phase 7.1 Closure` section appended. ROADMAP.md Phase 7 shows Complete with date, Phase 7.1 row is filled in with both plans listed and marked [x], and a new Progress table row tracks Phase 7.1 as 2/2 Complete. All three closure documents reflect the PASS outcome consistently.
  </done>
</task>

</tasks>

<verification>
Plan-level verification (full D-04 closure criterion):

1. **SC#3 verified green end-to-end:**
   - `.planning/phases/07-openai-oauth-flow/07-UAT.md` contains `## Re-verification (Phase 7.1 — final closure)` section with SC#3 result: PASS
   - `/tmp/sc3-rerun-07-1-02.sse` contains ≥1 `event: text_delta` line
   - 07-UAT.md frontmatter `status: PASS`

2. **SC#4 verified green end-to-end:**
   - Re-verification section documents oauthExpiry advanced (`after > before`)
   - SC#4 chat request contains ≥1 text_delta (completed during mid-flight refresh)

3. **Requirements closed:**
   - `grep '^- \[x\] \*\*OAUTH-02\*\*' .planning/REQUIREMENTS.md` matches
   - `grep '^- \[x\] \*\*OAUTH-03\*\*' .planning/REQUIREMENTS.md` matches
   - Traceability table shows both as "Complete"

4. **Verification status propagated:**
   - 07-VERIFICATION.md frontmatter `status: verified`, `score: 5/5`, `re_verification: true`
   - Both gaps marked `status: resolved`
   - `## Phase 7.1 Closure` section exists

5. **ROADMAP updated:**
   - Phase 7 row: `4/4 | Complete | <date>`
   - Phase 7.1 row: `2/2 | Complete | <date>` with both plans listed as [x]
   - No `TBD` remaining in Phase 7.1 section

6. **No gambiarra (guardrail):**
   - No new files in src/server/ named `codex-*`, `bypass-*`, or equivalent custom handlers
   - If patch-package used: `patches/@mariozechner+pi-ai+*.patch` exists, postinstall script in package.json, and diff is minimal (non-refactoring)
   - If Branch A: src/server/ changes are surgical (targeted at the errorMessage's root cause)

7. **Prior history preserved:**
   - 07-UAT.md still contains original "## Re-verification (Phase 7.1 gap closure)" section from Plan 07-04 (historical record intact)
</verification>

<success_criteria>
Plan 07.1-02 is complete when:

- [ ] Task 1 decision recorded (D-02 branch selected or escalation triggered)
- [ ] Task 2 fix applied per selected branch (src/server/ edit OR patches/@mariozechner+pi-ai+*.patch committed OR Outcome 1 skipped)
- [ ] No bypass adapter introduced (no new src/server/ files with codex-specific custom handler)
- [ ] SC#3 re-verified PASS — ≥1 event: text_delta in /api/chat with provider=openai + Codex model
- [ ] SC#4 re-verified PASS — force-expire + chat advances oauthExpiry timestamp
- [ ] 07-UAT.md has new `## Re-verification (Phase 7.1 — final closure)` section (original Re-verification section preserved)
- [ ] 07-UAT.md frontmatter changed to `status: PASS`
- [ ] 07-VERIFICATION.md frontmatter: `status: verified`, `score: 5/5`, `re_verification: true`, both gaps `status: resolved`
- [ ] 07-VERIFICATION.md has `## Phase 7.1 Closure` section
- [ ] REQUIREMENTS.md: OAUTH-02 and OAUTH-03 both `[x] Complete` in requirements list AND traceability table
- [ ] ROADMAP.md: Phase 7 row = `4/4 | Complete | <date>`; Phase 7.1 row = `2/2 | Complete | <date>` with both plans listed
- [ ] All four closure documents reflect PASS outcome consistently
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-solucao-do-problema-encontrado-e-documentado-na-fase-7/07.1-02-SUMMARY.md` documenting:
- D-02 branch selected in Task 1 (A/B/C/Outcome-1/escalation) and reasoning
- Exact fix applied in Task 2 (file paths + diff summary OR patch path + patched lines)
- SC#3 re-run curl output (first 20 lines) + text_delta count
- SC#4 before/after oauthExpiry timestamps + advancement confirmation
- 07-UAT.md re-verification section diff
- 07-VERIFICATION.md frontmatter changes
- REQUIREMENTS.md + ROADMAP.md updates
- Commits created
- Any deviations from plan (environmental issues, OAuth re-consent, etc.)
- Final assertion: OAUTH-02 and OAUTH-03 Complete, Phase 7 closed
</output>
