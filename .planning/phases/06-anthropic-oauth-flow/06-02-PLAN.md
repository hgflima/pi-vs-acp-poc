---
phase: 06-anthropic-oauth-flow
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/phases/06-anthropic-oauth-flow/06-UAT.md
autonomous: false
requirements: [OAUTH-01]
must_haves:
  truths:
    - "06-UAT.md exists documenting the end-to-end OAuth flow validation via curl (D-05)"
    - "UAT records the result of POST /api/chat with provider=anthropic after OAuth completion — proves SC#3 (OAuth token works for chat)"
    - "UAT records the port conflict test result (SC#4) — 409 with Claude Code CLI message"
    - "UAT outcome is explicitly marked: PASS (OAuth token works) or BLOCKED (token obtained but Anthropic API rejects per Feb 2026 ban)"
  artifacts:
    - path: ".planning/phases/06-anthropic-oauth-flow/06-UAT.md"
      provides: "End-to-end UAT record per D-05 covering SC#1-SC#4 from ROADMAP phase 6 success criteria"
      contains: "status:"
  key_links:
    - from: ".planning/phases/06-anthropic-oauth-flow/06-UAT.md"
      to: "POST /api/auth/oauth/start + GET /api/auth/oauth/status + POST /api/chat"
      via: "curl commands documented with actual outputs"
      pattern: "curl"
---

<objective>
Perform end-to-end UAT validation of the Anthropic OAuth flow via curl (D-05) and record the outcome in `06-UAT.md`. This is the empirical proof for SC#1 (auth URL exposed), SC#2 (status polling works + credentials stored), SC#3 (OAuth token works for real /api/chat call), and SC#4 (port conflict gives 409 with clear message). Per D-05, Phase 8 delivers the real UI; Phase 6 only needs to prove the credential plumbing works end-to-end through the backend.

Purpose: OAUTH-01's acceptance test. Unlike API Key auth (Phase 5 validates at storage time), OAuth tokens are inherently validated by the exchange step inside `loginAnthropic`, so the true test is whether the stored token actually works for a chat request. Per STATE.md: "Anthropic OAuth included despite research flagging potential ban — user decision to implement regardless." This checkpoint is where that risk materialises or is falsified.

Output: `.planning/phases/06-anthropic-oauth-flow/06-UAT.md` with status (PASS / BLOCKED / FAIL), curl commands run, actual responses captured, and follow-up notes.
</objective>

<execution_context>
@/Users/henriquelima/Documents/dev/personal/pi-ai-poc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henriquelima/Documents/dev/personal/pi-ai-poc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-anthropic-oauth-flow/06-CONTEXT.md
@.planning/phases/06-anthropic-oauth-flow/06-RESEARCH.md
@.planning/phases/06-anthropic-oauth-flow/06-01-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold 06-UAT.md template with SC#1-SC#4 sections and curl commands</name>
  <files>.planning/phases/06-anthropic-oauth-flow/06-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 6 section — must copy the four Success Criteria verbatim into the UAT doc)
    - .planning/phases/06-anthropic-oauth-flow/06-CONTEXT.md (D-05 for UAT scope; decisions reference)
    - .planning/phases/06-anthropic-oauth-flow/06-RESEARCH.md (Pitfall 1 — Anthropic ban risk that determines PASS vs BLOCKED outcome)
    - .planning/phases/06-anthropic-oauth-flow/06-01-SUMMARY.md (to know the exact endpoint paths and response shapes implemented)
  </read_first>
  <action>
    Create a NEW file at `.planning/phases/06-anthropic-oauth-flow/06-UAT.md` with the following exact frontmatter and structure. The human checkpoint in Task 2 will fill in the actual results — this task is the scaffold only.

    **Frontmatter (must be literal YAML):**
    ```yaml
    ---
    phase: 06-anthropic-oauth-flow
    type: uat
    status: pending
    executed: null
    scope: "End-to-end validation of Anthropic OAuth PKCE flow per D-05"
    requirements: [OAUTH-01]
    ---
    ```

    **Body sections in this exact order:**

    # Phase 6 UAT — Anthropic OAuth Flow

    ## Scope (from D-05)

    End-to-phase validation uses manual UAT via curl. Flow: start OAuth via curl POST /api/auth/oauth/start, complete consent in browser, then curl POST /api/chat with provider: "anthropic" and verify stream response. Re-uses the existing /api/chat route (no new test endpoints). Phase 8 delivers the real UI.

    ## Success Criteria (from ROADMAP.md Phase 6)

    - SC#1: Backend endpoint initiates Anthropic OAuth PKCE flow via pi-ai loginAnthropic and returns the auth URL to the frontend
    - SC#2: After user completes OAuth consent, backend stores the OAuth credentials and a status polling endpoint reports success
    - SC#3: User can send chat messages using the Anthropic OAuth token (verified with real API call)
    - SC#4: If port 53692 is occupied, the user gets a clear error message explaining the conflict

    ## Prerequisites

    - Backend running: `npm run dev:server` (port 3001)
    - Anthropic account with active Pro/Max subscription (required for OAuth — Claude.ai login)
    - Port 53692 free (check with `lsof -nP -iTCP:53692 -sTCP:LISTEN`)
    - Browser available to complete OAuth consent

    ## Test Plan

    ### Test 1 — SC#1: Start OAuth flow, receive auth URL

    ```bash
    curl -sS -X POST http://localhost:3001/api/auth/oauth/start \
      -H "Content-Type: application/json" \
      -d '{"provider":"anthropic"}' | jq .
    ```

    **Expected:** HTTP 200, JSON body `{"status":"started","provider":"anthropic","authUrl":"https://claude.ai/oauth/authorize?..."}` with a non-empty authUrl.

    **Actual:**
    ```
    [paste response here]
    ```

    **Result:** [ PASS | FAIL ]

    ---

    ### Test 2 — SC#2: Poll status while pending, complete browser flow, verify stored

    **Step 2a.** While the browser tab is still being opened, poll status:
    ```bash
    curl -sS "http://localhost:3001/api/auth/oauth/status?provider=anthropic" | jq .
    ```
    **Expected (while pending):** `{"status":"pending","provider":"anthropic","authUrl":"https://claude.ai/..."}`

    **Step 2b.** Open the authUrl from Test 1 in a browser. Complete Anthropic consent screen (sign in with Claude.ai credentials, approve the requested scopes). Browser should redirect to `http://127.0.0.1:53692/callback?...` and show pi-ai's built-in success page.

    **Step 2c.** Poll status again after consent completes:
    ```bash
    curl -sS "http://localhost:3001/api/auth/oauth/status?provider=anthropic" | jq .
    ```
    **Expected (after completion):** `{"status":"ok","provider":"anthropic"}` (entry is cleared from the Map after this first read).

    **Step 2d.** Verify credentials persisted via Phase 5's authoritative status endpoint:
    ```bash
    curl -sS "http://localhost:3001/api/auth/status?provider=anthropic" | jq .
    ```
    **Expected:** `{"hasApiKey":<bool>,"hasOAuth":true,"activeMethod":"oauth","oauthExpiry":<unix-ms>}`

    **Actual (2a):**
    ```
    [paste response here]
    ```
    **Actual (2c):**
    ```
    [paste response here]
    ```
    **Actual (2d):**
    ```
    [paste response here]
    ```

    **Result:** [ PASS | FAIL ]

    ---

    ### Test 3 — SC#3: Send chat message using OAuth token

    With OAuth credentials stored from Test 2, send a chat request:
    ```bash
    curl -sS -N -X POST http://localhost:3001/api/chat \
      -H "Content-Type: application/json" \
      -d '{
        "provider":"anthropic",
        "modelId":"claude-haiku-4-5-20251001",
        "messages":[{"role":"user","content":"Say OK and nothing else","timestamp":0}]
      }'
    ```

    **Expected (PASS):** SSE stream with assistant content events; stream completes with a `done` event carrying non-empty content including "OK".

    **Expected (BLOCKED, per Pitfall 1):** Error response or SSE error event containing text like "OAuth authentication is currently not supported" or "invalid authentication method". This is the documented Feb 2026 Anthropic policy rejecting `sk-ant-oat*` tokens for non-Claude-Code apps.

    **Actual:**
    ```
    [paste first 20-30 lines of stream response here]
    ```

    **Result:** [ PASS (token works end-to-end) | BLOCKED (upstream policy rejects — expected risk per STATE.md) | FAIL (plumbing bug) ]

    **If BLOCKED:** Record the exact error message. This is still a successful phase completion — the code is correct, the API policy is enforced. Note the outcome in 06-SUMMARY.md under "Known Upstream Limitations".

    ---

    ### Test 4 — SC#4: Port 53692 conflict returns 409 with Claude Code CLI message

    **Step 4a.** Occupy port 53692 with a stand-in listener:
    ```bash
    nc -l 53692 &
    NC_PID=$!
    sleep 1
    ```

    **Step 4b.** Trigger /start while port is busy:
    ```bash
    curl -sS -w "\nHTTP %{http_code}\n" -X POST http://localhost:3001/api/auth/oauth/start \
      -H "Content-Type: application/json" \
      -d '{"provider":"anthropic"}'
    ```

    **Expected:** HTTP 409, JSON body `{"status":"error","message":"Port 53692 is already in use by another process. This port is required by the Anthropic OAuth callback. If you have Claude Code CLI running, please stop it and try again."}`

    **Step 4c.** Clean up:
    ```bash
    kill $NC_PID 2>/dev/null
    ```

    **Actual:**
    ```
    [paste response + HTTP code here]
    ```

    **Result:** [ PASS | FAIL ]

    ---

    ## Final Outcome

    **Overall:** [ PASS | BLOCKED | FAIL ]

    **Summary:**
    - SC#1 (auth URL returned): [ PASS | FAIL ]
    - SC#2 (status polling + credentials stored): [ PASS | FAIL ]
    - SC#3 (OAuth token works for chat): [ PASS | BLOCKED | FAIL ]
    - SC#4 (port conflict handled): [ PASS | FAIL ]

    **Follow-up actions:**
    - [list any items, or "None"]

    **Executed by:** [name]
    **Executed at:** [ISO timestamp]

    ---

    Do NOT fill in the [paste ... here], [ PASS | FAIL ], or [name/timestamp] placeholders in this task — Task 2's human checkpoint fills them.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-anthropic-oauth-flow/06-UAT.md &amp;&amp; grep -q 'status: pending' .planning/phases/06-anthropic-oauth-flow/06-UAT.md &amp;&amp; grep -q 'Test 1 — SC#1' .planning/phases/06-anthropic-oauth-flow/06-UAT.md &amp;&amp; grep -q 'Test 4 — SC#4' .planning/phases/06-anthropic-oauth-flow/06-UAT.md &amp;&amp; grep -q 'Port 53692 is already in use' .planning/phases/06-anthropic-oauth-flow/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/06-anthropic-oauth-flow/06-UAT.md` returns 0
    - Frontmatter status: `grep -q '^status: pending$' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - Frontmatter type: `grep -q '^type: uat$' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - Frontmatter requirements: `grep -q 'requirements: \[OAUTH-01\]' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - All four test sections present: `grep -c "### Test [1-4] — SC#[1-4]" .planning/phases/06-anthropic-oauth-flow/06-UAT.md` returns 4
    - D-02 message in Test 4 expected: `grep -q "Port 53692 is already in use by another process. This port is required by the Anthropic OAuth callback. If you have Claude Code CLI running, please stop it and try again." .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - curl commands for /start: `grep -q 'POST http://localhost:3001/api/auth/oauth/start' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - curl commands for /status: `grep -q '/api/auth/oauth/status?provider=anthropic' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - curl commands for /api/chat: `grep -q 'POST http://localhost:3001/api/chat' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - BLOCKED outcome documented: `grep -q 'BLOCKED' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - Pitfall 1 referenced: `grep -q 'Feb 2026' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - Final Outcome section: `grep -q '## Final Outcome' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
  </acceptance_criteria>
  <done>
    Per D-05: UAT template scaffolded with all four SC tests, curl commands ready to run, BLOCKED outcome (per Pitfall 1) explicitly supported. Status: pending — awaiting human execution in Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT execution — run the four tests and fill in 06-UAT.md</name>
  <files>.planning/phases/06-anthropic-oauth-flow/06-UAT.md</files>
  <read_first>
    - .planning/phases/06-anthropic-oauth-flow/06-UAT.md (the scaffold from Task 1 — human fills it in)
  </read_first>
  <action>
    HUMAN VERIFICATION CHECKPOINT — this task pauses execution for the user to run the UAT manually. Executor must:
    1. Print the verification steps below to the user.
    2. Wait for the user to type "approved" / "PASS" / "BLOCKED" / "FAIL" or describe failures.
    3. Do NOT automate the OAuth consent flow — the user must complete it in a real browser against the live Anthropic OAuth service.
    4. Do NOT pre-fill the UAT doc — the user pastes real outputs as they run.

    **What is built (for user reference):** POST /api/auth/oauth/start and GET /api/auth/oauth/status endpoints (Plan 01) plus UAT scaffold (this plan, Task 1). Ready for end-to-end human validation per D-05.

    **Verification steps to present to the user:**

    1. **Ensure backend is running** in a separate terminal:
       ```
       npm run dev:server
       ```
       Confirm it prints `Backend running on http://localhost:3001`.

    2. **Verify port 53692 is free** before Test 1:
       ```
       lsof -nP -iTCP:53692 -sTCP:LISTEN
       ```
       Should output nothing (exit code 1). If a process holds it, stop it (common culprit: Claude Code CLI — run `pkill -f "claude"` if you're comfortable doing so).

    3. **Execute Test 1** (SC#1 — start OAuth, receive authUrl). Run the curl command from the UAT doc. Paste the JSON response into the "Actual" block. Mark PASS if authUrl is a non-empty URL starting with `https://claude.ai/oauth/authorize`; mark FAIL otherwise.

    4. **Execute Test 2** (SC#2 — status polling + credentials stored).
       - Run Step 2a curl while flow is pending; paste response. Should show `"status":"pending"`.
       - Open the authUrl from Test 1 in a browser; sign in with Claude.ai credentials; approve consent. pi-ai's built-in success page should appear.
       - Run Step 2c curl after consent completes; paste response. Should show `"status":"ok"`.
       - Run Step 2d curl; paste response. Should show `"hasOAuth":true,"activeMethod":"oauth"`.
       - Mark PASS if all three steps match expected; mark FAIL otherwise.

    5. **Execute Test 3** (SC#3 — send chat via OAuth token). Run the /api/chat curl. Paste the first 20-30 lines of the streamed response.
       - If the stream emits assistant content events with "OK" in the content → PASS
       - If the stream emits an error event with text like "OAuth authentication is currently not supported" or "invalid authentication method" → BLOCKED (per Pitfall 1, this is the documented Anthropic Feb 2026 policy; the code is correct, the API rejects) — document the exact error
       - If plumbing fails (500 error, connection refused, code-level bug) → FAIL

    6. **Execute Test 4** (SC#4 — port conflict). Start nc listener on 53692, run /start curl, confirm 409 + Claude Code CLI message, kill nc. Paste response and HTTP code.

    7. **Fill in Final Outcome** section at bottom of 06-UAT.md:
       - Set per-SC results
       - Set Overall: PASS if all four PASS, BLOCKED if SC#3 is BLOCKED and others PASS, FAIL if any plumbing failure
       - Record executed-by name and ISO timestamp
       - List any follow-up actions (or "None")

    8. **Update frontmatter** at top of 06-UAT.md:
       - Change `status: pending` to `status: PASS` or `status: BLOCKED` or `status: FAIL`
       - Change `executed: null` to `executed: <ISO timestamp>`

    **Acceptable outcomes for phase completion:**
    - PASS: all four SC verified. Phase 6 done.
    - BLOCKED: SC#1, SC#2, SC#4 PASS; SC#3 BLOCKED by Anthropic policy (expected risk, documented). Phase 6 done with note.
    - FAIL: Any plumbing-level bug. Do NOT mark phase complete — route back to planner for gap closure.
  </action>
  <verify>
    <automated>grep -q 'status: \(PASS\|BLOCKED\|FAIL\)' .planning/phases/06-anthropic-oauth-flow/06-UAT.md &amp;&amp; grep -q '\*\*Overall:\*\* \(PASS\|BLOCKED\|FAIL\)' .planning/phases/06-anthropic-oauth-flow/06-UAT.md &amp;&amp; ! grep -q '\[paste .* here\]' .planning/phases/06-anthropic-oauth-flow/06-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter status updated from 'pending': `grep -qE '^status: (PASS|BLOCKED|FAIL)$' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - Frontmatter executed timestamp set: `grep -qE '^executed: 20[0-9]{2}-' .planning/phases/06-anthropic-oauth-flow/06-UAT.md` (ISO date)
    - Overall outcome set: `grep -qE '\*\*Overall:\*\* (PASS|BLOCKED|FAIL)$' .planning/phases/06-anthropic-oauth-flow/06-UAT.md` (no brackets remaining)
    - No '[paste ... here]' placeholders left in Actual blocks: `! grep -q '\[paste' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - No '[ PASS | FAIL ]' unfilled result markers: `! grep -q '\[ PASS | FAIL \]' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - No '[name]' unfilled placeholder: `! grep -q '\[name\]' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`
    - If BLOCKED: exact Anthropic error message captured in Test 3 Actual block (human judgment — checker reviews content)
    - Resume signal received from user: one of "approved" / "PASS" / "BLOCKED" / "FAIL"
  </acceptance_criteria>
  <done>
    User executed all four UAT tests live, pasted real curl outputs into 06-UAT.md, marked each SC with PASS/BLOCKED/FAIL, updated frontmatter status + executed timestamp, and signalled completion. Phase 6 is declared complete if Overall is PASS or BLOCKED.
  </done>
  <resume-signal>Type "approved" (or "PASS" / "BLOCKED" / "FAIL" matching the filled-in Final Outcome), or describe any issues encountered during testing</resume-signal>
</task>

</tasks>

<verification>
**Structural verification (automated, post-Task-2):**
- `grep -qE '^status: (PASS|BLOCKED|FAIL)$' .planning/phases/06-anthropic-oauth-flow/06-UAT.md` — status was updated from "pending"
- `grep -c '\*\*Actual' .planning/phases/06-anthropic-oauth-flow/06-UAT.md` returns at least 4 — actual response blocks exist
- `grep -qE '\*\*Overall:\*\* (PASS|BLOCKED|FAIL)' .planning/phases/06-anthropic-oauth-flow/06-UAT.md` — Final Outcome filled in
- No remaining `[paste ... here]` placeholders: `! grep -q '\[paste' .planning/phases/06-anthropic-oauth-flow/06-UAT.md`

**Phase 6 declared complete when:**
- UAT status is PASS or BLOCKED (not FAIL, not pending)
- If BLOCKED: 06-SUMMARY.md notes the upstream policy limitation
</verification>

<success_criteria>
- 06-UAT.md exists with scaffolded tests for SC#1-SC#4
- Human executed all four tests and filled in actual responses
- Final Outcome is PASS or BLOCKED (FAIL means gap closure needed)
- Frontmatter status reflects the filled-in outcome
- OAUTH-01 is demonstrated end-to-end (or proven blocked by upstream policy)
</success_criteria>

<output>
After completion, create `.planning/phases/06-anthropic-oauth-flow/06-02-SUMMARY.md` documenting:
- UAT outcome (PASS / BLOCKED / FAIL)
- Per-SC results
- If BLOCKED: exact error message from Anthropic API, date observed, reference to Pitfall 1 in 06-RESEARCH.md
- Follow-up items for Phase 8 UI (e.g., "surface BLOCKED state in connection UI" if SC#3 blocked)
</output>
